<h1>to do list</h1>


<%= link_to 'add task', new_path %>

<% @tasks.each do |task| %>
 
  <h3><%=link_to task.title, task_path(task)  %>  <%= task.completed ? '✅': '❌'  %></h3>
  
  <p><%= task.details  %></p>
  
<% end %>

